# =============================================================================
# Confidential data — DO NOT PUBLISH
# =============================================================================
# The UC2 dataset is provided by Masabi under a data-use agreement and contains
# MBTA rider account_ids. None of the raw inputs or rider-level outputs may be
# committed to a public repository.
pipeline/data/
*.csv.gz

# Rider-level outputs (contain account_ids of suspected fraudsters)
pipeline/outputs/feature_table.*
pipeline/outputs/symbol_rows.*
pipeline/outputs/sequences.npz
pipeline/outputs/rider_scores.*
pipeline/outputs/uc2_human_review_shortlist*.csv
pipeline/outputs/uc2_hmm_only_riders*.csv
pipeline/outputs/uc2_rule_vs_hmm_overlap*.csv

# (Model-level outputs without rider identifiers — hmm_best.pkl,
#  hmm_emissions.csv, hmm_grid_results.csv — are committed.)

# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/
.env

# =============================================================================
# OS / IDE
# =============================================================================
.DS_Store
.vscode/
.idea/
*.swp
*~

# =============================================================================
# Build / dist
# =============================================================================
build/
dist/
*.egg
